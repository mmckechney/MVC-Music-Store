apiVersion: v1
kind: Service
metadata:
  name: {{ include "mvcmusicstoreweb.fullname" . }}
  labels:
    {{- include "mvcmusicstoreweb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mvcmusicstoreweb.selectorLabels" . | nindent 4 }}
