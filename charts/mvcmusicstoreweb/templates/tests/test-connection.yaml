apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mvcmusicstoreweb.fullname" . }}-test-connection"
  labels:
    {{- include "mvcmusicstoreweb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mvcmusicstoreweb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
